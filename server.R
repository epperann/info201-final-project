## The server for the app
## Information for building plots can be put here, but
## it may be cleaner if they are built in a separate
## .R file and sourced in.
library(shiny)
library(tidyverse)

server <- function(input, output){
  
}

shinyServer(server)